function makeMessage (name, price) {
    const result = `You picked ${name}, price per item is ${price} credits`
    return result
};



function makeMessage(name, price) {
    console.log(`You picked ${name}, price per item is ${price} credits`)
};

//my
function makeMessage(name, price) {
    const result = `You picked ${name}, price per item is ${price} credits`;
    console.log(result);
    return result;
}
