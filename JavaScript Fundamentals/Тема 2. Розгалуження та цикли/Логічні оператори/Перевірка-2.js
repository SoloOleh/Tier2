function checkAccess(subType) {
  if (subType === "pro" || subType === "vip") {
    return true;
  } else {
    return false;
  }
}

function checkAccess(subType) {
  if (subType === "pro" || "vip") {
    return true;
  } else {
    return false;
  }
}
