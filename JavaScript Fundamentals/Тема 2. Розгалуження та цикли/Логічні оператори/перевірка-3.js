function toggleModalVisibility(isVisible) {
    if (!isVisible === false) {
        return false;
    } else {
        return true;
    }
}

function toggleModalVisibility(isVisible) {
    if (!isVisible) {
        return true;
    } else {
        return false;
    }
}


function toggleModalVisibility(isVisible) {
  return !isVisible;
}


