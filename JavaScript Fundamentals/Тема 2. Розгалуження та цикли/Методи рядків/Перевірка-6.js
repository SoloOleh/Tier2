function createFileName(name, ext) {
  const trimmedName = name.trim();
  const trimmedExt = ext.trim();
  return trimmedName + "." + trimmedExt;
}

function createFileName(name, ext) {
  const trimmedName = name.trim();
  const fileName = trimmedName + "." + ext;
  return fileName;
}

function createFileName(name, ext) {
  const trimmedName = name.trim();
  return (fileName = trimmedName + "." + ext);
}

function createFileName(name, ext) {
  const trimmedName = name.trim();
  return (fileName = trimmedName + "." + ext);
}
